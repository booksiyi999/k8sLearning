# K8s Quest UX/交互设计分析报告

> 基于 `frontend/app.js`、`frontend/terminal.js`、`frontend/index.html`、`backend/app/cluster.py` 源码确认

---

## P0-1 章节门控把零基础新生锁在 ch00（体验断点）

### 1. 问题根因（设计层面）

**代码确认**（`app.js` L733-738）：

```js
isChapterUnlocked(chId) {
  const chNum = parseInt(chId.replace('ch', ''));
  if (isNaN(chNum) || chNum <= 0) return true;  // ch00 始终解锁
  const prevNum = chNum - 1;
  const prevLevels = this.levels.filter(l => l.id.startsWith(`Q${prevNum}.`));
  return prevLevels.length > 0 && prevLevels.every(l => this.progress.completed_levels.includes(l.id));
}
```

**根因**：门控策略采用「严格线性前置依赖」——上一章 **所有关卡** 必须 `completed` 才解锁下一章。这是一个「教学设计假设」与「用户心理模型」的错配：

| 维度 | 教学设计假设 | 用户实际行为 |
|------|------------|------------|
| 学习路径 | 必须循序渐进，不可跳跃 | 想先浏览全局，建立心智地图 |
| 首次体验 | 从 ch00 开始，3 关足够 sampling | 3 关太少，无法判断平台价值 |
| 动机模型 | 完成关卡 → 解锁 → 正反馈循环 | 看不到后续内容 → 疑虑 → 流失 |

UI 层面，`index.html` L86-100 中锁定章节仅显示 `🔒 完成上一章节解锁`，没有「预览」入口、没有「即将解锁」的引导，是一个 **hard wall**（硬墙）而非 **soft gate**（软门）。

### 2. 设计方案对比

| 方案 | 描述 | 优点 | 缺点 | 实现复杂度 |
|------|------|------|------|----------|
| **A. 自由浏览 + 练习锁定** | 所有章节可展开浏览标题/讲解，但未解锁章节的「动手练习」Tab 禁用并显示「完成前置章节解锁」 | 最大化探索自由度；讲解内容本身就是学习价值；消除「够不着」感 | 可能降低完成前置关卡的动机（"反正能看"）；需区分「浏览」与「练习」状态 | **低** — 改 `isChapterUnlocked` 逻辑拆分为 `canBrowse` / `canPractice`，练习 Tab 加 disabled 状态 |
| **B. 默认解锁前 N 章（渐进门控）** | 新用户默认解锁 ch00-ch02（约 15 关），后续章节仍需线性解锁 | 首次体验内容量从 3→15，足够 sampling；保留中后段的门控价值 | N 的选择需 A/B 测试；章节难度跨度大时可能让用户卡在 ch02 | **低** — `isChapterUnlocked` 加 `chNum <= 2 return true` 前置判断 |
| **C. 探索模式开关** | 顶部增加「🔓 探索模式」toggle，开启后所有章节可浏览+练习，关闭后恢复线性门控 | 灵活度最高；尊重不同学习风格；导师可按需开放 | 开关可能被忽略（discoverability 问题）；探索模式下失去「通关成就感」 | **中** — 新增全局状态 + localStorage 持久化 + 章节卡/UI 联动 |
| **D. ch00 扩展为 onboarding 大章** | ch00 从 3 关扩展到 8-10 关，涵盖 Pod/Deployment/Service 概览，让首次体验更充分 | 不破坏门控模型；首次体验内容量充足 | 内容制作成本高；仍无法满足"看后面"的需求 | **高** — 需编写新关卡 + 讲解 + check 逻辑 |

### 3. PM 推荐方案

**推荐：方案 A（自由浏览 + 练习锁定）为主，叠加方案 B 的前 2 章默认解锁**

理由：
1. **方案 A** 直接解决「够不着」的核心痛点——用户可以浏览所有章节的讲解内容（知识讲解 Tab），建立全局认知。练习锁定保留了「通关进度」的游戏化价值。
2. **叠加方案 B** 让 ch01-ch02 的练习也可用，首次可练习关卡从 3→15+，足够让用户感受平台的 check 机制和即时反馈。
3. 实现成本极低（改一个函数 + 加 CSS disabled 状态），可快速上线验证。
4. 保留了 `completed_levels` / 徽章 / 连击等游戏化系统的完整性——只有"练习并通过"才计入进度。

**关键交互细节**：
- 未解锁章节的练习 Tab 显示：`🔒 完成上一章所有关卡后解锁练习` + 「查看前置章节」按钮
- 未解锁章节的讲解 Tab 正常显示，但顶部加一行提示条：`📖 浏览模式 — 完成前置章节后可动手练习`
- 章节卡锁定状态从 `🔒` 改为 `👁️ 可浏览`，视觉上不再是"完全锁死"

### 4. 实现复杂度：**低**

---

## P0-2 终端 Tab 是真实 kubectl 代理（安全风险面）

### 1. 问题根因（设计层面）

**代码确认**：

- `terminal.js` L225-253：`termRun()` → `termExec()` → `POST /api/kubectl` 真实执行
- `cluster.py` L382-386：`DANGEROUS_SUBCOMMANDS = {delete, drain, cordon, uncordon, taint, scale, rollout, edit, exec}`
- `cluster.py` L388-390：`FORBIDDEN_SUBCOMMANDS = {destroy, reset, init}` — 仅拦截 kubeadm 级别
- `terminal.js` L210-219：`termIsDangerous()` 前端预判，命中后显示确认条（L436-446）
- `index.html` L370-378：模拟器模式下有黄色警告条，但 **集群模式下无任何风险提示**

**根因**：三层设计缺陷叠加：

| 层次 | 问题 |
|------|------|
| **讲解层** | ch21 讲解含 `mv /var/lib/etcd`、`kubeadm upgrade` 等操作，讲解文档无 ⚠️ 风险标注，用户可能直接复制到终端执行 |
| **终端层** | 集群模式下终端 Tab 无风险横幅；危险命令确认条是 inline 的（在终端输出流中），不够醒目；确认后 `force=true` 直接执行，无二次校验 |
| **系统层** | `FORBIDDEN_SUBCOMMANDS` 只拦截 `kubeadm init/reset/destroy`，但不拦截 `rm -rf`、`mv`、shell 注入等；`exec` 子命令在 dangerous 而非 forbidden |

### 2. 设计方案对比

| 方案 | 描述 | 优点 | 缺点 | 实现复杂度 |
|------|------|------|------|----------|
| **A. 终端默认只读 + 实操模式开关** | 终端默认只读（只允许 get/describe/explain/logs），顶部增加「⚡ 实操模式」开关，开启后才允许写操作 | 安全默认值（secure by default）；写操作需用户主动 opt-in；符合最小权限原则 | 开关可能被忽略；切换后仍需危险命令确认 | **中** — 新增 `termReadOnly` 状态 + 开关 UI + 后端 `read_only` 参数过滤 |
| **B. 危险操作确认对话框（Modal）** | 危险命令不执行 inline 确认条，改为弹出 Modal 对话框，显示命令全文 + 风险等级 + 影响范围 | 比 inline 确认条更醒目；可携带风险描述（"此操作将删除资源，不可恢复"）；Modal 打断力强 | 频繁弹出可能造成"确认疲劳"；用户可能养成无脑点确认的习惯 | **低** — 将 `termConfirmCmd` 的 inline bar 改为 Modal overlay |
| **C. 风险横幅 + 命令分级标注** | 终端顶部常驻风险横幅（`⚠️ 你正在操作真实集群 [namespace: xxx]`）；每条命令执行前显示风险等级标签（🟢安全/🟡需注意/🔴危险） | 持续提醒，不依赖单次确认；命令分级让用户建立风险感知；视觉成本低 | 横幅可能被"广告盲区"忽略；需维护命令分级规则 | **低** — 加横幅 HTML + 命令分级函数 |
| **D. 讲解层风险标注 + 终端拦截联动** | 讲解文档中对破坏性操作加 `> ⚠️ **危险操作**：此命令会...` 标注；终端拦截讲解中标注为"危险"的命令模式 | 从源头降低误操作风险；讲解和终端形成安全闭环 | 需逐章修订讲解文档；命令模式匹配可能有遗漏 | **高** — 讲解修订 + 命令模式库 |

### 3. PM 推荐方案

**推荐：方案 C（风险横幅 + 命令分级标注）+ 方案 B（Modal 确认）组合，同步推进方案 D 的讲解层标注**

理由：
1. **方案 C 的风险横幅**是"始终在线"的安全提醒，不依赖用户行为触发，是安全 UX 的 baseline。当前集群模式下终端完全无提示（`index.html` L382 直接显示终端），这是最大的设计缺陷。
2. **方案 B 的 Modal 确认**替换当前的 inline 确认条（`term-confirm-bar`），增强打断力。当前 inline 确认条在终端输出流中，视觉层级太低。
3. **方案 D 的讲解层标注**从源头降低风险——ch21 讲解中的 `mv /var/lib/etcd` 等操作如果不加标注，用户很可能直接复制到终端。这是"防御纵深"的第一道防线。
4. 方案 A（只读模式）作为后续迭代考虑——当前 ch28 CKA 关需要写操作，只读模式会破坏功能完整性。

**关键交互细节**：
- **风险横幅**（常驻终端顶部）：
  ```
  ⚠️ 你正在操作真实 K8s 集群 | Namespace: k8s-quest-student | 模式: 集群实战
  ```
  背景色 `#fff3cd`（warning yellow），不可关闭
- **命令分级标签**（每条命令执行后显示）：
  - 🟢 `get/describe/logs/explain` → 安全
  - 🟡 `apply/create/expose` → 写操作
  - 🔴 `delete/drain/scale/exec` → 危险（触发 Modal 确认）
- **Modal 确认对话框**：
  ```
  ⚠️ 确认执行危险命令？
  
  命令: kubectl delete pod nginx
  风险等级: 🔴 危险 — 此操作将删除资源，不可恢复
  影响范围: Namespace k8s-quest-student
  
  [取消]  [确认执行]
  ```
- **讲解层标注**：破坏性操作段落加引用块 + ⚠️ 图标 + 背景色

### 4. 实现复杂度：**中**（横幅+Modal 低，讲解标注高）

---

## P1-1 ch28 CKA 关 example 全失败

### 1. 问题根因（设计层面）

**代码确认**（`backend/tests/test_ch28_cluster.py`）：

- ch28 是集群验证关，`/api/check/cluster` 端点要求真实 `kubectl` 命令执行
- 模拟器模式下 `/api/check/cluster` 返回 `"集群模式未启用"`（L27）
- 模拟器模式下常规 `/api/check` 走关键词匹配（L51-59），但 ch28 的 example 是命令片段（如 `kubectl run nginx --image=nginx`），模拟器的 YAML 解析器无法处理

**根因**：**答案模态与验证模态不匹配**。ch28 的设计意图是"命令型答案"（CKA 考试风格），但模拟器验证器只支持"YAML 型答案"。这是一个 **内容设计** 与 **平台能力** 的错配：

| 维度 | 设计意图 | 平台能力 |
|------|---------|---------|
| 答案格式 | kubectl 命令行 | YAML manifest |
| 验证方式 | 真实集群状态检查 | 模拟器状态机 |
| 模拟器模式 | 应支持命令模拟 | 不支持命令解析 |

### 2. 设计方案对比

| 方案 | 描述 | 优点 | 缺点 | 实现复杂度 |
|------|------|------|------|----------|
| **A. 模拟器模式降级为"概念理解"关** | 模拟器模式下 ch28 自动转换为概念选择题/填空题，集群模式下保持命令型验证 | 快速修复；不影响集群模式体验；概念题也有学习价值 | 降低了 CKA 实战感；需编写概念题内容 | **中** — 编写概念题 + 模拟器模式 check 分支 |
| **B. 终端命令模拟器** | 模拟器模式下增加一个轻量 kubectl 命令解析器，支持 `kubectl run/create/expose/get` 等基础命令的模拟执行和验证 | 保持命令型答案的一致体验；用户可在模拟器中练习 kubectl 语法 | 模拟器复杂度大增；命令覆盖率有限；维护成本高 | **高** — 需开发命令解析 + 模拟执行引擎 |
| **C. 标注 + 跳过机制** | ch28 在模拟器模式下显示「⚠️ 此章节需集群模式」横幅，允许跳过并标注"需集群环境验证" | 实现简单；诚实告知用户；不产生误判 | 用户体验差（5 关全不能做）；降低平台完整性 | **低** — 前端条件渲染 + 跳过逻辑 |

### 3. PM 推荐方案

**推荐：方案 A（模拟器模式降级为概念关）**

理由：
1. 方案 C 虽然简单，但 5 关全跳过对用户体验伤害太大，且校招生大多在模拟器模式。
2. 方案 B 技术成本过高，ROI 不合理。
3. 方案 A 让模拟器用户也能"做"ch28，只是形式从"敲命令"变成"选概念"，保留了学习闭环。集群模式用户仍可享受完整 CKA 体验。
4. 概念题可以覆盖 CKA 核心知识点（etcd 备份、集群升级、故障排查流程），学习价值不打折。

**交互细节**：模拟器模式下 ch28 关卡显示提示条 `ℹ️ 当前为模拟器模式，本章以概念题形式呈现。配置集群模式可体验真实 CKA 命令操作。`

### 4. 实现复杂度：**中**

---

## P1-2 ch17 概念关 example 不过（3/10）

### 1. 问题根因（设计层面）

**根因**：与 P1-1 类似的模态错配，但方向相反。ch17 的 Q17.6（Reconcile）/ Q17.8（Finalizer）/ Q17.10（最佳实践）是 **概念理解关**，用户应该通过选择/描述概念来通关，但 example 提供的是 **YAML 片段**，验证器走的是 YAML 结构检查，片段不满足完整 check。

这是 **关卡类型分类** 的缺失——平台没有区分"YAML 实操关"和"概念理解关"，所有关卡共用同一套 YAML 验证流程。

### 2. 设计方案对比

| 方案 | 描述 | 优点 | 缺点 | 实现复杂度 |
|------|------|------|------|----------|
| **A. 引入关卡类型标签 + 概念关专用验证器** | 关卡增加 `type: yaml/concept/command` 标签；概念关使用选择题/简答题验证器 | 从架构上解决模态错配；可扩展到更多概念关；用户体验清晰 | 需改造关卡数据结构 + 验证器分发逻辑 | **中** — 数据结构 + 验证器路由 + 前端题型渲染 |
| **B. 概念关改为"YAML 标注题"** | 概念关仍用 YAML 编辑器，但要求用户在给定 YAML 中标注/修改特定字段（如"给这个资源加上 finalizer"） | 复用现有 YAML 编辑器；保持 UI 一致性 | 不是所有概念都能用 YAML 表达（如 Reconcile 流程）；限制了题目设计空间 | **低** — 改 check 逻辑 + 提供模板 YAML |
| **C. 概念关接受文本答案 + 关键词匹配** | 概念关改为文本输入框，check 逻辑做关键词/语义匹配 | 灵活度高；可覆盖任意概念题 | 关键词匹配容易误判；用户表述多样导致通过率低 | **低** — 前端加文本输入 + check 关键词列表 |

### 3. PM 推荐方案

**推荐：方案 A（关卡类型标签 + 概念关专用验证器），短期先用方案 C 过渡**

理由：
1. 方案 A 是正确的架构方向——不同题型应该有不同的验证模态。但实现周期较长。
2. 短期用方案 C（文本 + 关键词匹配）快速修复 Q17.6/Q17.8/Q17.10，让 example 能通过。
3. 方案 B 限制了概念题的表达力，不推荐。

**交互细节**：概念关的练习 Tab 显示文本输入框（而非 YAML 编辑器），placeholder 为「请用你自己的话描述...」，提交后显示关键词匹配反馈。

### 4. 实现复杂度：**中**（方案 A）/ **低**（方案 C 过渡）

---

## P2-1 3 关误判错答为通过

### 1. 问题根因（设计层面）

**根因**：验证器 check 逻辑过于宽松——可能是以下原因之一：
- 只检查了关键字段存在性（如"有 `spec.containers` 就 ok"），未验证字段值正确性
- 缺少负面校验（如"镜像必须是 nginx"但没检查"镜像不能是 busybox"）
- 容错逻辑过度（如 try-catch 吞掉了校验失败异常，默认返回 ok=True）

从设计角度看，这是 **验证反馈的信任设计** 问题：用户提交错误答案被判通过，会严重破坏平台可信度。用户会怀疑"其他通过的关是不是也是误判？"，动摇整个学习闭环的信任基础。

### 2. 设计方案对比

| 方案 | 描述 | 优点 | 缺点 | 实现复杂度 |
|------|------|------|------|----------|
| **A. 验证器加固 + 负面测试** | 逐关审查 check 逻辑，增加负面校验（错误答案必须返回 ok=False）；编写 negative test case | 从根本上修复误判；测试覆盖防止回归 | 需逐关审查 150 关 check 逻辑；工作量大 | **高** — 逐关审查 + 测试编写 |
| **B. 提交前预检 + 置信度标注** | 提交答案后，验证器返回置信度（high/medium/low），低置信度结果标注"⚠️ 请人工确认" | 快速覆盖；不阻塞用户；渐进改善 | 用户可能忽略标注；不修复根本问题 | **中** — 验证器增加置信度字段 + 前端标注 |
| **C. 3 关专项修复 + 验证器审计清单** | 只修复报告中的 3 关误判；建立验证器审计 checklist 供后续关卡参照 | 快速止血；成本可控；建立规范 | 其他关卡可能也有误判未发现 | **低** — 3 关 check 逻辑修复 |

### 3. PM 推荐方案

**推荐：方案 C（3 关专项修复）立即执行，后续逐步推进方案 A**

理由：
1. 3 关误判是 P2 级别，止血优先。方案 C 最快。
2. 建立验证器审计 checklist，新关卡上线前必须通过"错误答案必须 fail"的 negative test。
3. 方案 B 的置信度方案过于复杂，且对用户认知负担大，不推荐。

**交互细节**：无需前端改动。后端 check 逻辑增加负面校验分支即可。

### 4. 实现复杂度：**低**（3 关修复）/ **高**（全量审计）

---

## P2-2 ch25（659字）/ch07/ch26 讲解偏薄

### 1. 问题根因（设计层面）

**根因**：讲解内容（Lesson）是每关独立维护的，没有内容质量基线标准。659 字的讲解对于一个 K8s 章节（可能包含多个子主题）来说确实偏薄。从设计角度看，这是 **内容完整性设计规范** 的缺失。

### 2. 设计方案对比

| 方案 | 描述 | 优点 | 缺点 | 实现复杂度 |
|------|------|------|------|----------|
| **A. 讲解模板 + 最小字数门槛** | 定义讲解模板（概念→字段说明→示例→最佳实践→常见坑），每关讲解不低于 1500 字 | 结构化保证质量；可量化检查 | 模板可能限制表达自由；字数不等于质量 | **低** — 编写模板 + CI 检查 |
| **B. 讲解不足提示 + 外链补充** | 讲解字数不足时，前端自动显示「📖 想深入了解？推荐阅读：[官方文档链接]」 | 快速弥补；引导用户到权威来源 | 外链跳出降低留存；不解决内容质量问题 | **低** — 前端条件渲染 + 外链配置 |
| **C. 内容补充 sprint** | 专项补充 ch25/ch07/ch26 讲解内容，每关扩充到 1500-2000 字 | 直接解决；内容质量可控 | 内容创作成本高；需领域专家参与 | **中** — 内容编写 + review |

### 3. PM 推荐方案

**推荐：方案 C（内容补充）+ 方案 A（模板规范）**

理由：
1. 讲解是学习平台的核心价值，659 字确实不够。方案 C 直接解决。
2. 方案 A 建立模板规范，防止未来新关卡出现同样问题。
3. 方案 B 作为过渡——在外链补充配置好之前，先让用户知道"这里内容待补充"。

**交互细节**：讲解不足时，底部显示提示条 `📚 本节讲解正在完善中，如需更深入了解请参考 [官方文档]`，并显示「📝 反馈讲解不足」按钮。

### 4. 实现复杂度：**中**（内容编写）/ **低**（模板+提示）

---

## 优先级排序与实施建议

| 优先级 | 问题 | 推荐方案 | 复杂度 | 预估工期 |
|--------|------|---------|--------|---------|
| **P0-1** | 章节门控 | 自由浏览+练习锁定 + 前2章默认解锁 | 低 | 1-2 天 |
| **P0-2** | 终端安全 UX | 风险横幅 + Modal确认 + 讲解标注 | 中 | 3-5 天 |
| **P1-1** | ch28 CKA 关 | 模拟器降级为概念关 | 中 | 2-3 天 |
| **P1-2** | ch17 概念关 | 短期文本+关键词，长期关卡类型标签 | 低/中 | 1-2 天（过渡）/ 5+ 天（架构） |
| **P2-1** | 误判错答 | 3关专项修复 + 审计checklist | 低 | 1 天 |
| **P2-2** | 讲解偏薄 | 内容补充 + 模板规范 | 中 | 3-5 天（内容） |

---

## 附录：源码确认摘要

| 问题 | 文件 | 行号 | 确认内容 |
|------|------|------|---------|
| P0-1 | `app.js` | L733-738 | `isChapterUnlocked` 严格线性前置依赖 |
| P0-1 | `index.html` | L86-100 | 锁定章节显示 `🔒 完成上一章节解锁`，无预览入口 |
| P0-2 | `terminal.js` | L225-253 | `termRun` → `termExec` → `POST /api/kubectl` 真实执行 |
| P0-2 | `cluster.py` | L382-390 | `DANGEROUS_SUBCOMMANDS` / `FORBIDDEN_SUBCOMMANDS` 定义 |
| P0-2 | `index.html` | L370-378 | 模拟器模式有警告，集群模式无任何提示 |
| P0-2 | `index.html` | L436-446 | 危险命令确认条是 inline 的，非 Modal |
| P1-1 | `test_ch28_cluster.py` | L19-28 | 模拟器模式下 `/api/check/cluster` 返回未启用 |
| P1-2 | `app.js` | L113-119 | `loadLevel` → `/api/level/{id}` 无关卡类型区分 |
| P2-1 | `simulator.py` | 多处 | check 逻辑分布各关，无统一负面校验框架 |
